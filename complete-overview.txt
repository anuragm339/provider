graph TB
    subgraph Repositories
        direction TB
        Common[message-common]
        Provider[message-provider]
        Consumer[message-consumer-sdk]
    end

    subgraph Architecture
        subgraph Server[Server Components]
            MS[Message Service]
            DS[Delivery Service]
            BS[Bootstrap Service]
            DB[(SQLite + RocksDB)]
        end

        subgraph Client[Client Components]
            CC[Client SDK]
            LC[(Local Cache)]
            HR[Handler Registry]
        end
    end

    subgraph Features[Key Features]
        F1[Message Delivery]
        F2[Bootstrap Process]
        F3[Recovery Mechanism]
        F4[Data Cleanup]
    end

    Common --> Provider
    Common --> Consumer
    Provider --> Server
    Consumer --> Client
